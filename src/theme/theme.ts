import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: { main: "#5E56E7" },
    background: { default: "#F8F7FF" },
    text: { primary: "#333333", secondary: "#A0A0A0" },
  },
  typography: {
    fontFamily: "Montserrat, sans-serif",
  },
});
